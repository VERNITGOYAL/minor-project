import { useRef, useState } from "react";
import { Check, FileText, Upload, X } from "lucide-react";
const initialPapers = [{ name: "Attention_Is_All_You_Need.pdf", size: "4.2 MB" }, { name: "BERT_Research_Paper.pdf", size: "3.8 MB" }, { name: "YOLOv8_Paper.pdf", size: "5.1 MB" }];
function UploadPapers() { const inputRef = useRef(null); const [papers, setPapers] = useState(initialPapers); const [dragging, setDragging] = useState(false); const [processed, setProcessed] = useState(false); const addFiles = (files) => { const pdfs = Array.from(files || []).filter((file) => file.type === "application/pdf"); setPapers((current) => [...current, ...pdfs.map((file) => ({ name: file.name, size: `${(file.size / 1048576).toFixed(1)} MB` }))]); setProcessed(false); }; return <section className="px-4 pb-10 sm:px-8 lg:px-12"><div className="mx-auto max-w-4xl py-8 sm:py-12"><h1 className="text-2xl font-bold tracking-tight text-[#173c5d] sm:text-3xl">Upload Research Papers</h1><p className="mt-2 text-sm text-[#788598]">Add PDFs to your workspace and process them for research assistance.</p><div className={`mt-7 rounded-xl border-2 border-dashed p-7 text-center sm:p-12 ${dragging ? "border-[#398798] bg-[#edf6fa]" : "border-[#d5e0e5] bg-white"}`} onDragOver={(event) => { event.preventDefault(); setDragging(true); }} onDragLeave={() => setDragging(false)} onDrop={(event) => { event.preventDefault(); setDragging(false); addFiles(event.dataTransfer.files); }}><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-[#e8f4f7] text-[#398798]"><Upload size={29} /></div><h2 className="mt-4 text-base font-bold text-[#233044]">Drag & drop PDF files here</h2><p className="my-2 text-xs text-[#9aa7b6]">or</p><button type="button" className="rounded-md bg-[#173c5d] px-6 py-2.5 text-xs font-bold text-white" onClick={() => inputRef.current?.click()}>Choose Files</button><input ref={inputRef} className="sr-only" type="file" accept="application/pdf" multiple onChange={(event) => addFiles(event.target.files)} /><p className="mt-5 text-[10px] text-[#9aa7b6]">Supported format: PDF · Max size: 50MB per file</p></div><div className="mt-7 flex items-center justify-between"><h2 className="text-base font-bold text-[#233044]">Uploaded Papers ({papers.length})</h2><button type="button" className="text-xs font-bold text-[#536477]" onClick={() => setPapers([])}>Clear All</button></div><div className="mt-3 grid gap-2">{papers.map((paper) => <div className="flex min-h-14 items-center gap-3 border border-[#e1e7ec] bg-white px-3" key={`${paper.name}-${paper.size}`}><div className="grid h-9 w-9 place-items-center bg-[#fff0f1] text-[#e4515c]"><FileText size={18} /></div><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold text-[#405369]">{paper.name}</p><p className="text-[10px] text-[#8997a6]">{paper.size}</p></div><button type="button" onClick={() => setPapers((current) => current.filter((item) => item !== paper))} aria-label={`Remove ${paper.name}`}><X size={15} /></button></div>)}</div><button type="button" disabled={!papers.length} onClick={() => setProcessed(true)} className="mt-7 flex min-h-12 w-full items-center justify-center gap-2 bg-[#173c5d] text-sm font-bold text-white disabled:bg-[#b9c7cf]">{processed ? <><Check size={17} /> Papers ready for analysis</> : "Process Papers"}</button></div></section>; }
export default UploadPapers;
